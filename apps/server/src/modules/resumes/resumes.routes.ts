import { Router } from "express";

export const resumesRouter = Router();

// TODO(Phase 4 — see ARCHITECTURE.md §8, §7.2): upload to Cloudinary, enqueue onto
// parseResumeQueue (apps/server/src/queue/queues.ts), list/delete
